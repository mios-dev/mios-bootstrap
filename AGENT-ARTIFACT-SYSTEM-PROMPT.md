# External Artifact Agent System Prompt

Canonical copy: [mios-dev/MiOS AGENT-ARTIFACT-SYSTEM-PROMPT.md](https://github.com/mios-dev/MiOS/blob/main/AGENT-ARTIFACT-SYSTEM-PROMPT.md).

> This file is a pointer to the canonical artifact agent system prompt in `mios.git` per Architectural Law 8 (SSOT-PROJECTION) and Law 15 (DOUBLE-REPO-TRIPLE-CHECK). Refer to `AGENT-ARTIFACT-SYSTEM-PROMPT.md` at the root of `mios.git` for the complete external artifact agent contract.
