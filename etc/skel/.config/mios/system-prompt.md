# Per-user system prompt override
#
# Resolution order (first match wins):
#   $MIOS_AI_SYSTEM_PROMPT → this file → /etc/mios/ai/system-prompt.md → /usr/share/mios/ai/system.md
#
# Edit below to extend or override the canonical prompt for your session.
# Run `mios reinit-user-space` to reset to the system default.
