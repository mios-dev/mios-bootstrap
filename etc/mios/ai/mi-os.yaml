# MiOS Inference Configuration
# Relational Documentation for local LLM orchestration.

inference:
  provider: localai
  endpoint: http://localhost:8080
  schema: /v1/chat/completions
  inventory: /v1/models
